# 纸的厚度
n = 0.1
w = n
for i in range(50):
    w *= 2
    print(w)


# 国王麦粒
# 1:1 2:2 3:4 4:8
g = 1  # 当前格子应该放的麦粒数
total = 0  # 总麦粒数
a = 1  # 棋盘的格子数量
while a <= 100:
    total += g  # 计算当前的总麦粒数
    a += 1  # 走到下一个格子
    g *= 2  # 当前格子应该放的麦粒数 * 2
